# =============================================================================
# MAIN APPLICATION SETTINGS
# =============================================================================
# MongoDB Connection (Used by FastAPI application)
MONGODB_URI=mongodb://code_analysis_user:code_analysis_password@localhost:27018/ai-sdlc--agent-poc
MONGODB_DB_NAME=ai-sdlc--agent-poc

# API Settings
REPOSITORY_INGEST_API_URL=http://ingest-api-url/api/v1/repository-ingest

# Application Settings
DEBUG=false
LOG_LEVEL=INFO

# =============================================================================
# MONGODB INITIALIZATION SETTINGS (Used by mongo_init.py only)
# =============================================================================
# MongoDB Root Credentials (for initialization)
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=rootpassword

# MongoDB Connection Details (for initialization)
MONGO_HOST=localhost
MONGO_PORT=27018
